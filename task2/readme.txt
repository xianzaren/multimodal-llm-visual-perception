Task 2: Gradient comparison

This directory contains utilities for generating paired-region gradient-comparison stimuli and calculating prediction accuracy/error rates.

Large benchmark datasets, fine-tuning archives, prompts, and model outputs are hosted at https://osf.io/y4pgm/ rather than duplicated in Git.

Several scripts are research snapshots and contain relative paths matching the original experiment workspace. Update input/output paths before running them.
